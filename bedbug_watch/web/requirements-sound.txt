# Только для пересборки звука тревоги (sound/make_birds.py).
# Самому сторожу ничего из этого не нужно — он играет готовый mp3.
numpy>=1.24
soundfile>=0.12      # умеет писать mp3; ogg в Safari на iOS не играет
