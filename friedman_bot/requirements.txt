python-telegram-bot==20.7
python-dotenv==1.0.0
playwright==1.48.0
httpx==0.24.1
Pillow>=10.0      # карточка утреннего дайджеста Продавца (render_sales_card)

# Голос (TTS). Ставится автоматически командой /setupvoice; здесь для справки:
# edge-tts         # основной бесплатный движок — нейроголоса Microsoft (нужен интернет)
# omegaconf        # офлайн-движок Silero через torch.hub
# torch            # уже установлен для whisper (STT)
# imageio-ffmpeg   # уже установлен — конвертация в OGG/Opus
